//! edaptor — a schema-driven OpenLDAP TUI. M1 exposes a headless check pipeline.

pub mod config;
// `pub mod ldap;` is added in Task 4, when the ldap module is created, so every
// commit compiles.
